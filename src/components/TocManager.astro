/* src/components/TableOfContents.less */

/* 建议在您的全局样式中定义这些CSS变量
  :root {
    --vh-main-color: #3b82f6; // 主题色，用于激活状态的竖线 (示例: blue-500)
    --vh-text-color-1: #1f2937; // 主要文字颜色，用于标题和激活/悬浮状态 (示例: gray-800)
    --vh-text-color-2: #6b7280; // 次要文字颜色，用于非激活的目录项 (示例: gray-500)
  }
*/

.toc-wrapper {
  position: sticky;
  top: 80px;
}

.toc-title {
  font-size: 12px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em; /* 字母间距 */
  color: var(--vh-text-color-1, #1f2937);
  margin: 0 0 1rem 0;
  padding: 0;
}

.toc-list {
  list-style: none;
  padding-left: 0;
  margin: 0;
  max-height: calc(100vh - 180px);
  overflow-y: auto;
}

.toc-item a {
  display: block;
  text-decoration: none;
  padding: 4px 0; /* 调整垂直内边距 */
  /* 使用 border-left 来实现激活状态的竖线，更简洁高效 */
  border-left: 2px solid transparent;
  transition: all 0.2s ease-in-out;
}

.toc-text {
  display: block;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  transition: color 0.2s ease-in-out;
}

/* --- 层级样式：缩进和字体粗细 --- */
/* 默认 (h3 及以下) */
.toc-item a {
    padding-left: 1rem;
    font-size: 14px;
    font-weight: 400;
    color: var(--vh-text-color-2, #6b7280);
}
/* h1 (在目录中通常为最高级) */
.toc-item.depth-1 a {
    font-weight: 500;
}
/* h2 */
.toc-item.depth-2 a {
    /* 保持默认样式 */
}
/* h3 */
.toc-item.depth-3 a {
    padding-left: 2rem;
}
/* h4 */
.toc-item.depth-4 a {
    padding-left: 3rem;
}
/* h5 */
.toc-item.depth-5 a {
    padding-left: 4rem;
}


/* --- 悬浮效果：仅改变文字颜色 --- */
.toc-item a:hover {
  color: var(--vh-text-color-1, #1f2937);
}

/* --- 激活效果：改变文字颜色并显示左侧竖线 --- */
.toc-item a.active {
  color: var(--vh-text-color-1, #1f2937); /* 激活时文字变深色 */
  font-weight: 500; /* 激活时字体加粗一点 */
  border-left-color: var(--vh-main-color, #3b82f6); /* 显示蓝色的竖线 */
}
